# Daily Progress - 2026-03-30

## Summary

Today closed out the Tenderly underwriting smoke work by proving the remaining
dispute path on a clean child fork and documenting the full recovery path.

## Completed

- validated the v2 recovery plan against a fresh Tenderly VNet
- restaged a clean parent baseline through deployment, verification, and
  operator setup
- verified the correct fork-readiness invariants for a clean parent:
  - `jobCounter=0`
  - coordinator nonce must be read live and reused as
    `SETTLEMENT_ESCROW_NONCE_START`
- ran the one-stage dispute flow successfully on a clean child fork
- confirmed the full live smoke matrix is now complete:
  - one-stage happy
  - two-stage happy
  - one-stage dispute
- wrote and pushed the final v2 completion docs

## Blockers Hit

### Tenderly quota exhaustion

- Tenderly repeatedly returned `HTTP 403` quota errors during live dispute-path
  attempts
- the same quota gate later blocked `evm_revert` on an earlier parent VNet

### Dirty child lineage

- one child fork was not usable because it already had `jobCounter=1`
- that proved it inherited scenario mutation and was not a clean dispute-only
  target

## Resolutions

- stopped using Tenderly as the debug loop and kept debugging local
- switched to a clean-parent and clean-child execution model
- required pre-run validation on every child fork:
  - deployed contracts exist
  - `jobCounter=0`
  - coordinator nonce is read live
- used `SETTLEMENT_ESCROW_NONCE_START=1` on the successful dispute child fork

## Artifacts Added Or Finalized

- `docs/plans/2026-03-29-tenderly-forked-smoke-results.md`
- `docs/plans/2026-03-29-tenderly-forked-smoke-execution-notes.md`
- `docs/daily/2026-03-30.md`

## Current State

- branch includes the v2 recovery plan, results, and execution notes
- the Tenderly smoke matrix is complete
- the repo now has a dedicated `docs/daily` folder for daily summaries

## Next Useful Daily Entry Pattern

Future daily notes should keep the same structure:

- summary
- completed work
- blockers hit
- resolutions
- artifacts added or changed
- current state
